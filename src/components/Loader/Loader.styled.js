import styled from 'styled-components';

export const LoaderWrap = styled('div')({
  position: 'absolute',
  top: '0',
  left: '0',
  width: '100%',
  height: '100%',
  background: '#424242',
  opacity: '1',
  zIndex: '10',
});
